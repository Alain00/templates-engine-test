{
  "name": "pug-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "pug src -o dist -P",
    "build:watch": "pug src -o dist -w -P"
  },
  "devDependencies": {
    "pug": "^3.0.2"
  }
}
